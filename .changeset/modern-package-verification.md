---
"@kasoa/vite-plus-config": patch
---

Migrate the Cloudflare Workers test integration to `@cloudflare/vitest-plugin` and enable strict Publint and ESM-only ATTW checks in the library pack defaults.
