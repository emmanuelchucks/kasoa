---
"@kasoa/env": patch
"@kasoa/oxlint-config": patch
---

Require explicit env input in `@kasoa/env` and tighten the shared Oxlint presets with stricter native rules, type-aware defaults, and better test-file handling.
