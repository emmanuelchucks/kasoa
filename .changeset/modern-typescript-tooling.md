---
"@kasoa/eslint-config": patch
---

Declare the TypeScript 5 and 6 compatibility range required by the legacy `typescript-eslint` compiler API and direct new TypeScript 7 projects to the Vite+ config.
