---
"@kasoa/oxlint-config": patch
---

Ignore legacy JavaScript config files (`*.config.js`, `*.config.cjs`, `*.config.mjs`) in shared Oxlint presets to reduce noise from Expo and tooling config files.
